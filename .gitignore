terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.*

# root-level Terraform state (if present)
terraform.tfstate
terraform.tfstate.*

# (intentionally track Lambda ZIPs and ML tarballs in this repo)